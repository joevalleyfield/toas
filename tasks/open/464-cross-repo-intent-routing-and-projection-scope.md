## Goal
keywords: surface, explore, parked, research, cross-repo, intent, routing, projection

Define scope/routing semantics for intents that span multiple repositories/workspaces without violating local history invariants.

## Why

Cross-repo mission workflows were identified as a follow-on at `456` closure.

## Scope

- define cross-repo intent scope model and identifiers
- define safe projection/selection semantics across workspace boundaries
- define guardrails so local durable history remains canonical per repo

## Done When

- cross-repo intent model is documented with concrete invariants
- implementation tasks are split/estimated (or bounded prototype lands)
