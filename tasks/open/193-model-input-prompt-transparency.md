## Goal

Make model-facing prompt authority easier to inspect.

## Scope

- improve prompt/input inspection where needed
- ensure model-input views show what is actually being sent
- avoid hiding prompt-bearing layers behind helper abstractions

## Done When

- a user can inspect the effective model-facing prompt surface without guessing
