## Goal

Keep prompt assets in the repo, but treat them as reusable source material rather than silent runtime policy.

## Scope

- clarify the role of prompt assets in docs and code
- keep protocol/prompt variants available for explicit use
- avoid implying that prompt assets are always active

## Done When

- prompt assets are described as library material or explicitly selected variants
- runtime behavior does not depend on hidden prompt injection
