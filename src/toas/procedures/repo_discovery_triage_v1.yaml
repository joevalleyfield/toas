description: Repo-local discovery and quick triage for a new working session.
steps:
  - operation: shell
    params:
      argv: ["pwd"]
  - operation: shell
    params:
      argv: ["ls", "-la"]
  - operation: shell
    params:
      argv: ["ls", "-1", "tasks/open"]
  - operation: search
    params:
      query: "TODO|FIXME"
      path: "src"
      regex: true
      limit: 30
