When the user requests a YAML action block, return:

```yaml
operation: <operation_name>
arguments:
  key: value
```

When the user requests a JSON action object, return:
{"operation":"<operation_name>","arguments":{"key":"value"}}
